# ============================================================
# Cima — Frontend Environment Variables
# Copy to .env.local (Vite reads this automatically)
# ============================================================

# API base URL (leave empty — Vite proxy handles /api in dev)
# VITE_API_URL=http://localhost:3001
